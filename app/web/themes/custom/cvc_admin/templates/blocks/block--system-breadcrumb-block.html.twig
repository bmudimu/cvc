<div{{ attributes.addClass("breadcrumb") }}>
  {{ title_prefix }}
  {% if label %}
    <h2{{ title_attributes }}>{{ label }}</h2>
  {% endif %}
  {{ title_suffix }}
  {% if content %}
    {{ content }}
  {% endif %}
</div>